<view class="container">
  <image bind:tap="onLeft" class="arrow" src="{{latest?leftArrowDis:leftArrow}}" />
  <text class="title animated bounceInUp">{{title}}</text>
  <image bind:tap="onRight" class="arrow" src="{{first?rightArrowDis:rightArrow}}" />
</view>